// https://leetcode-cn.com/problems/sudoku-solver/solution/
/**
 * @param {character[][]} board
 * @return {void} Do not return anything, modify board in-place instead.
 */
var solveSudoku = function (board) {

};
const grids = [], // 保存每一个九宫格的数字
    rows = [], // 保存每一行的数字
    columns = []; // 保存每一列的数字
const init = (board) => {
    for (let i = 0; i < 9; i++) {
        for (let j = 0; j < 9; j++) {

        }
    }
};